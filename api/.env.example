DATABASE_URL=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority
PORT=3000
FRONTEND_URL=http://localhost:4321

# MongoDB Atlas: Network Access must allow your IP (or 0.0.0.0/0 for dev).
# Without this, the API exits on startup with MongoServerSelectionError / ECONNRESET.
